# =============================================================================
# WinReverseAgent .gitignore — 根目录白名单策略
# 项目根最初被 System32 全目录误拷污染过，白名单法从源头防复发：
# 根目录只允许下列条目，其余任何新增文件自动忽略。
# =============================================================================

# —— 根目录白名单（显式放行）——
/*
!/README.md
!/DEVELOPMENT.md
!/运行说明.md
!/pyproject.toml
!/uv.lock
!/winreverse.spec
!/.gitignore
!/.python-version
!/.github/
!/src/
!/tests/
!/scripts/
!/skills/
!/assets/
!/vendor/
!/packages/
!/tools/
# tools/ 只放行清单文件；下载的工具二进制/目录仍忽略（避免把 tshark.exe 之类提交进仓库）
/tools/*
!/tools/manifest.yaml

# —— 忽略（白名单外的根目录与常见垃圾）——
!/*_backup*    # 暂不启用；保持严格白名单

# —— 通用忽略 ——
dist/
build/
output/
*.egg-info/
__pycache__/
*.py[cod]
.mypy_cache/
.pytest_cache/
.ruff_cache/
.venv/
# 注：tools/ 的忽略规则已上移到根目录白名单块（见 `!/tools/` + `/tools/*` + `!/tools/manifest.yaml`），
# 此处不再重复整目录忽略，否则会覆盖白名单、导致 tools/manifest.yaml 永远提交不进去。
.bak/
_*.py
!__init__.py
!_base.py
_*.ps1
_*.txt
config.toml
*.log
Thumbs.db
.DS_Store
